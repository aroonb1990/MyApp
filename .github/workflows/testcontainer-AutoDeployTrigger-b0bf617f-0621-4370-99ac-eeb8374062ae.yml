name: Trigger auto deployment for testcontainer

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'C:/Users/Admin/vruum-dotnetcore-api - Copy (2)/**'
    - '.github/workflows/testcontainer-AutoDeployTrigger-b0bf617f-0621-4370-99ac-eeb8374062ae.yml'

  # Allow manual trigger 
  workflow_dispatch:
      
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.TESTCONTAINER_AZURE_CREDENTIALS }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v1
        with:
          appSourcePath: ${{ github.workspace }}C:\Users\Admin\vruum-dotnetcore-api - Copy (2) 
          registryUrl: localhost:5000
          registryUsername: ${{ secrets.TESTCONTAINER_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.TESTCONTAINER_REGISTRY_PASSWORD }}
          containerAppName: testcontainer
          resourceGroup: VruumTest
          imageToBuild: localhost:5000/testcontainer:${{ github.sha }}
          dockerfilePath: Dockerfile

     

